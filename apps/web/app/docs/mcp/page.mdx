export const metadata = {
  title: "MCP Setup",
  description: "Add the caelus chart tools to Claude Desktop, Claude Code, Cursor, VS Code, and any MCP client, with verification, troubleshooting, and version pinning.",
  alternates: { canonical: "/docs/mcp" },
};

<div className="eyebrow" style={{ marginBottom: "0.6rem" }}>Integrations</div>

# MCP Setup

`caelus-mcp` exposes the engine over the Model Context Protocol. Positions are
computed by the engine on each call, never recalled from training data, so every
answer is deterministic.

There are two ways to connect, with the same twenty-five tools either way:

- **Hosted (Streamable HTTP)**: point your client at
  `https://www.ephemengine.com/api/mcp`. Nothing to install.
- **Local (stdio)**: run `npx -y caelus-mcp`. Best for offline use, version
  pinning, or air-gapped setups.

## Hosted endpoint (no install)

The tools are served over MCP Streamable HTTP at
`https://www.ephemengine.com/api/mcp`. The endpoint is stateless: each request
is self-contained, so there is no session to manage. Positions are identical to
the stdio server.

<Tabs>
  <Tab label="Cursor">

Add to `.cursor/mcp.json` (project) or `~/.cursor/mcp.json` (all projects), then
reload Cursor:

<CodeBlock
  lang="json"
  label=".cursor/mcp.json"
  code={`{
  "mcpServers": {
    "caelus": {
      "url": "https://www.ephemengine.com/api/mcp"
    }
  }
}`}
/>

  </Tab>
  <Tab label="Claude Code">

Add the remote server from the CLI:

<CodeBlock
  lang="bash"
  label="Terminal"
  code={`claude mcp add --transport http caelus https://www.ephemengine.com/api/mcp`}
/>

  </Tab>
  <Tab label="VS Code">

Create `.vscode/mcp.json`. VS Code uses `servers` and an explicit `type`:

<CodeBlock
  lang="json"
  label=".vscode/mcp.json"
  code={`{
  "servers": {
    "caelus": {
      "type": "http",
      "url": "https://www.ephemengine.com/api/mcp"
    }
  }
}`}
/>

  </Tab>
  <Tab label="curl">

The endpoint speaks JSON-RPC 2.0. A bare `tools/call` works without a prior
handshake because the server is stateless:

<CodeBlock
  lang="bash"
  label="Terminal"
  code={`curl -s https://www.ephemengine.com/api/mcp \\
  -H 'Content-Type: application/json' \\
  -H 'Accept: application/json, text/event-stream' \\
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/call",
       "params":{"name":"natal_chart",
                 "arguments":{"date":"1990-06-10T14:30:00Z","lat":27.95,"lon":-82.46}}}'`}
/>

A plain `GET` on the endpoint returns a small descriptor (tools, resources,
prompts) so you can confirm it is live.

  </Tab>
</Tabs>

## Run it locally

The published binary runs straight from npm, no global install:

<CodeBlock lang="bash" label="Terminal" code={`npx -y caelus-mcp`} />

It waits for an MCP client to speak to it over stdio, so an empty terminal is
expected. Wire it into a client below instead of running it by hand.

## Configure your client

Every client points at the same command (`npx -y caelus-mcp`); only the config
file and key differ.

<Tabs>
  <Tab label="Claude Desktop">

Edit `claude_desktop_config.json` (macOS:
`~/Library/Application Support/Claude/`, Windows: `%APPDATA%\Claude\`), then fully
quit and reopen Claude.

<CodeBlock
  lang="json"
  label="claude_desktop_config.json"
  code={`{
  "mcpServers": {
    "caelus": {
      "command": "npx",
      "args": ["-y", "caelus-mcp"]
    }
  }
}`}
/>

  </Tab>
  <Tab label="Claude Code">

Add it from the CLI; it writes the config for you:

<CodeBlock lang="bash" label="Terminal" code={`claude mcp add caelus -- npx -y caelus-mcp`} />

  </Tab>
  <Tab label="Cursor">

Create `.cursor/mcp.json` in the project (or `~/.cursor/mcp.json` for all
projects), then reload Cursor.

<CodeBlock
  lang="json"
  label=".cursor/mcp.json"
  code={`{
  "mcpServers": {
    "caelus": {
      "command": "npx",
      "args": ["-y", "caelus-mcp"]
    }
  }
}`}
/>

  </Tab>
  <Tab label="VS Code">

Create `.vscode/mcp.json` in the workspace; note VS Code uses the `servers`
key, not `mcpServers`:

<CodeBlock
  lang="json"
  label=".vscode/mcp.json"
  code={`{
  "servers": {
    "caelus": {
      "command": "npx",
      "args": ["-y", "caelus-mcp"]
    }
  }
}`}
/>

  </Tab>
  <Tab label="Generic">

Any stdio MCP client takes the same command and arguments:

<CodeBlock
  lang="json"
  label="mcp config"
  code={`{
  "command": "npx",
  "args": ["-y", "caelus-mcp"]
}`}
/>

  </Tab>
</Tabs>

## Verify the tools appeared

After the client reloads, it should list **twenty-five** tools. Ask it to enumerate
them, or check the client's MCP panel:

`natal_chart`, `current_sky`, `transits`, `synastry`, `find_aspect_dates`,
`rectification_grid`, `sky_events` (rise/set, phases, stations, zodiac
crossings, solar and lunar eclipses), `planetary_hours`, `void_of_course`,
`returns` (solar/lunar return instants plus the first return chart),
`progressions` (secondary progressions and solar-arc directions), `composite`
(midpoint composite and the Davison chart), `dignities` (essential dignity
and sect for the seven traditional planets), `lots` (the seven Hermetic
lots, sect-aware), `profections` (annual and monthly profections, lord of the
year), `firdaria` (the planetary time-lord periods), `releasing` (zodiacal
releasing / aphesis from the Lot of Spirit or Fortune), `directions`
(primary directions of the planets to the angles, and optionally between the
planets), `nakshatras` (lunar mansions, padas, and lords), `dasha` (Vimshottari,
Yogini, or Ashtottari periods), `vargas` (Parashari divisional charts),
`yogas` (Pancha Mahapurusha, raja/dhana yogas, and yogakarakas),
`aspect_patterns` (the classical configurations: T-squares, grand trines and
crosses, yods, kites, mystic rectangles, and stelliums, as maximal structured
objects), and `chart_signature` (element, modality, quadrant, and hemisphere
distributions, the dominant element, modality, and sign, and the classical
chart ruler).

`natal_chart` and `current_sky` also tag each body with its solar phase
(cazimi, combust, under the beams) and each aspect as applying or separating.

If you see all twenty-five, the server is wired correctly. The server also exposes two
**resources**: `caelus://glossary` (aspect angles and orbs, signs, bodies,
house systems, essential dignities) and `caelus://accuracy` (the validation
table), plus a **prompt**, `rectification_session`, a guided flow around
`rectification_grid` and `find_aspect_dates`.

A natal-chart call returns structured positions, not prose:

<CodeBlock
  lang="json"
  label="natal_chart → (abbreviated)"
  code={`{
  "utc": "1990-06-10T14:30:00Z",
  "houses": "placidus",
  "bodies": {
    "sun":  { "lon": 79.45,  "pos": "19°27'Gem", "house": 11, "speed": 0.96 },
    "moon": { "lon": 283.28, "pos": "13°17'Cap", "house": 6,  "speed": 12.19 },
    "saturn": { "lon": 294.31, "pos": "24°18'Cap", "house": 6, "rx": true, "speed": -0.05 }
  }
}`}
/>

## Example prompts

- "Natal chart: born June 10 1990, 2:30pm, Tampa FL."
- "When is Saturn square my natal Moon in the next two years?"
- "Compare my chart with my partner's."
- "Next solar eclipse? Lunar eclipses in 2026?"
- "My solar return chart for this year."
- "Progress my chart to today: secondary positions and the solar arc."
- "Composite and Davison charts for me and my partner."
- "Which of my planets have essential dignity, and are they in sect?"
- "Where are my Lots of Fortune and Spirit?"
- "Who's my lord of the year for 2026?"
- "Which firdaria period am I in right now?"
- "Run my zodiacal releasing from the Lot of Spirit."
- "What are my primary directions to the angles?"
- "Do I have any T-squares, grand trines, or yods in my chart?"
- "What's the elemental and modal balance of my chart, and my chart ruler?"

## Troubleshooting

- **`npx` not found / server never starts.** GUI clients (Claude Desktop,
  Cursor) do not inherit your shell `PATH`, so a `node`/`npx` installed via nvm
  or Homebrew may be invisible. Use the absolute path as `command` (find it with
  `which npx`), or install Node where the client can see it. Node 18+ is
  required.
- **Tools don't show up.** Most clients only read MCP config on a full restart.
  Quit completely (not just close the window) and reopen. Confirm the JSON
  parses (a trailing comma is the usual culprit).
- **First call is slow.** `npx` downloads the package on first run; it is cached
  afterward. Pinning a version (below) avoids surprise re-downloads.

## Version pinning for production

`npx -y caelus-mcp` always resolves the latest release. Pin an exact version so
behaviour is reproducible and the four packages stay in lockstep:

<CodeBlock
  lang="json"
  label="pinned"
  code={`{
  "mcpServers": {
    "caelus": {
      "command": "npx",
      "args": ["-y", "caelus-mcp@0.15.0"]
    }
  }
}`}
/>

Agent-oriented docs live at <a href="/llms.txt">llms.txt</a>. See
<a href="/docs/architecture">Architecture</a> for where MCP fits in an
end-to-end app.
